info:
  name: processing jobs
  type: http
  seq: 13

http:
  method: GET
  url: "{{api_url}}/jobs/?status=processing"
  params:
    - name: status
      value: processing
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
