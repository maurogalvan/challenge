info:
  name: get jobs
  type: http
  seq: 11

http:
  method: GET
  url: "{{api_url}}/jobs/{{job_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
