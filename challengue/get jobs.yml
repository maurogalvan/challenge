info:
  name: get jobs
  type: http
  seq: 8

http:
  method: GET
  url: "{{api_url}}/jobs/<job-id>/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
