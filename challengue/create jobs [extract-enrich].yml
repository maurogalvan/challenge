info:
  name: "create jobs [full pipeline]"
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/jobs/"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "document_name": "sample.txt",
        "document_type": "text/plain",
        "content": "contenido simulado del documento",
        "pipeline_config": {
          "stages": ["extract", "analyze", "enrich"],
          "provider_overrides": {
            "extract": "fast",
            "analyze": "slow",
            "enrich": "fast"
          }
        }
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  `extract`+`enrich` sin `analyze` en medio no es válido; este request es el pipeline completo.
