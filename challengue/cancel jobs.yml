info:
  name: cancel jobs
  type: http
  seq: 9

http:
  method: POST
  url: "{{api_url}}/jobs/<job-id>/cancel/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
